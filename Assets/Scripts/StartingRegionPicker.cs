using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using EAB.Game;

public class StartingRegionPicker : MonoBehaviour
{
    Infection infection;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
